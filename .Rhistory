# Append the extracted data to the existing_data data frame
#existing_data <- rbind(existing_data, extracted_data)
}
print(packets_file)
print(length(packets_file))
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
print(packets_data)
# Extract the 'total packets' column
total_packets <- packets_data$`Total Packets`
# Add 'total packets' column to the extracted_data data frame
extracted_data$network <- total_packets
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
}
print(length(packets_file))
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
print(packets_data)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
print(packets_data)
# Extract the 'total packets' column
total_packets <- packets_data$`Total Packets`
# Add 'total packets' column to the extracted_data data frame
extracted_data$network <- total_packets
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
}
# Extract the 'total packets' column
total_packets <- packets_data$`Total Packets`
print(total_packets)
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
print(length(packets_file))
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
print(packets_data)
# Extract the 'total packets' column
total_packets <- packets_data$`Total Packets`
print(total_packets)
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
print(length(packets_file))
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
print(packets_data)
# Extract the 'total packets' column
total_packets <- packets_data$TotalPackets
print(total_packets)
print(packets_data)
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
print(length(packets_file))
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
print(packets_data)
# Extract the 'total packets' column
total_packets <- packets_data$`Total Packets`
print(total_packets)
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
print(length(packets_file))
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
print(packets_data)
# Extract the 'total packets' column
total_packets <- packets_data$Total Packets
print(total_packets)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
print(total_packets)
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
print(packets_data)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
# Add 'total packets' column to the extracted_data data frame
extracted_data$network <- total_packets
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
}
# Append the extracted data to the existing_data data frame
#existing_data <- rbind(existing_data, extracted_data)
}
# Loop through each directory
for (dir in directories) {
# Extract digits from the directory name
digits <-as.numeric(gsub("\\D", "", basename(dir)))  # Using 'basename' to get the name of the directory
# Check if there are at least 6 digits in the directory name
if (nchar(digits) >= 6) {
# Extract the first six digits for run_id, microphone, camera, screen, duration, and app
run_id <- substr(digits, 1, 1)
microphone <- substr(digits, 2, 2)
camera <- substr(digits, 3, 3)
screen <- substr(digits, 4, 4)
duration <- substr(digits, 5, 5 )
app <- substr(digits, 6, 6)
# Create a data frame with the extracted digits
extracted_data <- data.frame(run_id, microphone, camera, screen, duration, app, stringsAsFactors = FALSE)
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
print(packets_data)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
# Add 'total packets' column to the extracted_data data frame
extracted_data$network <- total_packets
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
}
# Append the extracted data to the existing_data data frame
#existing_data <- rbind(existing_data, extracted_data)
}
}
# Read specific columns from data.csv
existing_data <- read.csv("data-analysis/data.csv", header = TRUE, stringsAsFactors = FALSE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
# Set the path to the directory containing subdirectories
data_folder <- "data-analysis//data"
# List all directories inside the 'data' folder
directories <- list.dirs(path = data_folder, full.names = TRUE, recursive = FALSE)
# Loop through each directory
for (dir in directories) {
# Extract digits from the directory name
digits <-as.numeric(gsub("\\D", "", basename(dir)))  # Using 'basename' to get the name of the directory
# Check if there are at least 6 digits in the directory name
if (nchar(digits) >= 6) {
# Extract the first six digits for run_id, microphone, camera, screen, duration, and app
run_id <- substr(digits, 1, 1)
microphone <- substr(digits, 2, 2)
camera <- substr(digits, 3, 3)
screen <- substr(digits, 4, 4)
duration <- substr(digits, 5, 5 )
app <- substr(digits, 6, 6)
# Create a data frame with the extracted digits
extracted_data <- data.frame(run_id, microphone, camera, screen, duration, app, stringsAsFactors = FALSE)
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
print(packets_data)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
# Add 'total packets' column to the extracted_data data frame
extracted_data$network <- total_packets
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
}
}
}
# Write the extracted data to the existing CSV file
write.csv(existing_data, file = "data-analysis/data.csv", row.names = FALSE, quote = FALSE)
# Check the updated contents of the CSV file
read.csv("data-analysis/data.csv")
# Read specific columns from data.csv
existing_data <- read.csv("data-analysis/data.csv", header = TRUE, stringsAsFactors = FALSE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
# Set the path to the directory containing subdirectories
data_folder <- "data-analysis//data"
# List all directories inside the 'data' folder
directories <- list.dirs(path = data_folder, full.names = TRUE, recursive = FALSE)
# Loop through each directory
for (dir in directories) {
# Extract digits from the directory name
digits <-as.numeric(gsub("\\D", "", basename(dir)))  # Using 'basename' to get the name of the directory
# Check if there are at least 6 digits in the directory name
if (nchar(digits) >= 6) {
# Extract the first six digits for run_id, microphone, camera, screen, duration, and app
run_id <- substr(digits, 1, 1)
microphone <- substr(digits, 2, 2)
camera <- substr(digits, 3, 3)
screen <- substr(digits, 4, 4)
duration <- substr(digits, 5, 5 )
app <- substr(digits, 6, 6)
# Create a data frame with the extracted digits
extracted_data <- data.frame(run_id, microphone, camera, screen, duration, app, stringsAsFactors = FALSE)
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
print(packets_data)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
# Add 'total packets' column to the extracted_data data frame
extracted_data$network <- total_packets
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
}
}
}
# Write the extracted data to the existing CSV file
write.csv(existing_data, file = "data-analysis/data.csv", row.names = FALSE, quote = FALSE)
# Read specific columns from data.csv
existing_data <- read.csv("data-analysis/data.csv", header = TRUE, stringsAsFactors = FALSE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
# Set the path to the directory containing subdirectories
data_folder <- "data-analysis//data"
# List all directories inside the 'data' folder
directories <- list.dirs(path = data_folder, full.names = TRUE, recursive = FALSE)
# Loop through each directory
for (dir in directories) {
# Extract digits from the directory name
digits <-as.numeric(gsub("\\D", "", basename(dir)))  # Using 'basename' to get the name of the directory
# Check if there are at least 6 digits in the directory name
if (nchar(digits) >= 6) {
# Extract the first six digits for run_id, microphone, camera, screen, duration, and app
run_id <- substr(digits, 1, 1)
microphone <- substr(digits, 2, 2)
camera <- substr(digits, 3, 3)
screen <- substr(digits, 4, 4)
duration <- substr(digits, 5, 5 )
app <- substr(digits, 6, 6)
# Create a data frame with the extracted digits
extracted_data <- data.frame(run_id, microphone, camera, screen, duration, app, stringsAsFactors = FALSE)
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
# Add 'total packets' column to the extracted_data data frame
extracted_data$network <- total_packets
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
}
}
}
# Write the extracted data to the existing CSV file
write.csv(existing_data, file = "data-analysis/data.csv", row.names = FALSE, quote = FALSE)
# Read specific columns from data.csv
existing_data <- read.csv("data-analysis/data.csv", header = TRUE, stringsAsFactors = FALSE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
# Set the path to the directory containing subdirectories
data_folder <- "data-analysis//data"
# List all directories inside the 'data' folder
directories <- list.dirs(path = data_folder, full.names = TRUE, recursive = FALSE)
# Loop through each directory
for (dir in directories) {
# Extract digits from the directory name
digits <-as.numeric(gsub("\\D", "", basename(dir)))  # Using 'basename' to get the name of the directory
# Check if there are at least 6 digits in the directory name
if (nchar(digits) >= 6) {
# Extract the first six digits for run_id, microphone, camera, screen, duration, and app
run_id <- substr(digits, 1, 1)
microphone <- substr(digits, 2, 2)
camera <- substr(digits, 3, 3)
screen <- substr(digits, 4, 4)
duration <- substr(digits, 5, 5 )
app <- substr(digits, 6, 6)
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
# Add 'total packets' column to the extracted_data data frame
extracted_data$network <- total_packets
}
# Create a data frame with the extracted digits
extracted_data <- data.frame(run_id, microphone, camera, screen, duration, app, network, stringsAsFactors = FALSE)
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
}
}
# Write the extracted data to the existing CSV file
write.csv(existing_data, file = "data-analysis/data.csv", row.names = FALSE, quote = FALSE)
# Check the updated contents of the CSV file
read.csv("data-analysis/data.csv")
# Read specific columns from data.csv
existing_data <- read.csv("data-analysis/data.csv", header = TRUE, stringsAsFactors = FALSE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
# Set the path to the directory containing subdirectories
data_folder <- "data-analysis//data"
# List all directories inside the 'data' folder
directories <- list.dirs(path = data_folder, full.names = TRUE, recursive = FALSE)
# Loop through each directory
for (dir in directories) {
# Extract digits from the directory name
digits <-as.numeric(gsub("\\D", "", basename(dir)))  # Using 'basename' to get the name of the directory
# Check if there are at least 6 digits in the directory name
if (nchar(digits) >= 6) {
# Extract the first six digits for run_id, microphone, camera, screen, duration, and app
run_id <- substr(digits, 1, 1)
microphone <- substr(digits, 2, 2)
camera <- substr(digits, 3, 3)
screen <- substr(digits, 4, 4)
duration <- substr(digits, 5, 5 )
app <- substr(digits, 6, 6)
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
# Add 'total packets' column to the extracted_data data frame
network <- total_packets
}
# Create a data frame with the extracted digits
extracted_data <- data.frame(run_id, microphone, camera, screen, duration, app, network, stringsAsFactors = FALSE)
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
}
}
# Write the extracted data to the existing CSV file
write.csv(existing_data, file = "data-analysis/data.csv", row.names = FALSE, quote = FALSE)
# Check the updated contents of the CSV file
read.csv("data-analysis/data.csv")
# Read specific columns from data.csv
existing_data <- read.csv("data-analysis/data.csv", header = TRUE, stringsAsFactors = FALSE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
# Set the path to the directory containing subdirectories
data_folder <- "data-analysis//data"
# List all directories inside the 'data' folder
directories <- list.dirs(path = data_folder, full.names = TRUE, recursive = FALSE)
# Loop through each directory
for (dir in directories) {
# Extract digits from the directory name
digits <-as.numeric(gsub("\\D", "", basename(dir)))  # Using 'basename' to get the name of the directory
# Check if there are at least 6 digits in the directory name
if (nchar(digits) >= 6) {
# Extract the first six digits for run_id, microphone, camera, screen, duration, and app
run_id <- substr(digits, 1, 1)
microphone <- substr(digits, 2, 2)
camera <- substr(digits, 3, 3)
screen <- substr(digits, 4, 4)
duration <- substr(digits, 5, 5 )
app <- substr(digits, 6, 6)
}
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
# Add 'total packets' column to the extracted_data data frame
network <- total_packets
}
# Create a data frame with the extracted digits
extracted_data <- data.frame(run_id, microphone, camera, screen, duration, app, network, stringsAsFactors = FALSE)
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
}
# Write the extracted data to the existing CSV file
write.csv(existing_data, file = "data-analysis/data.csv", row.names = FALSE, quote = FALSE)
# Check the updated contents of the CSV file
read.csv("data-analysis/data.csv")
# Read specific columns from data.csv
existing_data <- read.csv("data-analysis/data.csv", header = TRUE, stringsAsFactors = FALSE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
# Set the path to the directory containing subdirectories
data_folder <- "data-analysis//data"
# List all directories inside the 'data' folder
directories <- list.dirs(path = data_folder, full.names = TRUE, recursive = FALSE)
# Loop through each directory
for (dir in directories) {
print(dir)
# Extract digits from the directory name
digits <-as.numeric(gsub("\\D", "", basename(dir)))  # Using 'basename' to get the name of the directory
# Check if there are at least 6 digits in the directory name
if (nchar(digits) >= 6) {
# Extract the first six digits for run_id, microphone, camera, screen, duration, and app
run_id <- substr(digits, 1, 1)
microphone <- substr(digits, 2, 2)
camera <- substr(digits, 3, 3)
screen <- substr(digits, 4, 4)
duration <- substr(digits, 5, 5 )
app <- substr(digits, 6, 6)
}
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
# Add 'total packets' column to the extracted_data data frame
network <- total_packets
}
# Create a data frame with the extracted digits
extracted_data <- data.frame(run_id, microphone, camera, screen, duration, app, network, stringsAsFactors = FALSE)
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
}
# Write the extracted data to the existing CSV file
write.csv(existing_data, file = "data-analysis/data.csv", row.names = FALSE, quote = FALSE)
# Read specific columns from data.csv
existing_data <- read.csv("data-analysis/data.csv", header = TRUE, stringsAsFactors = FALSE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
print(existing_data)
# Read specific columns from data.csv
existing_data <- read.csv("data-analysis/data.csv", header = TRUE, stringsAsFactors = FALSE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
print(existing_data)
# Read specific columns from data.csv
existing_data <- read.csv("data-analysis/data.csv", header = TRUE, stringsAsFactors = FALSE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
print(existing_data)
# Read specific columns from data.csv
existing_data <- read.csv("data-analysis/data.csv", header = TRUE, stringsAsFactors = TRUE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
print(existing_data)
data_file <- read.csv("data-analysis/data.csv")
data_file
existing_data <- read.csv(data_file, header = TRUE, stringsAsFactors = FALSE,
colClasses = c("character", "character", "character",
"character", "character", "character", "character"))
print(existing_data)
existing_data <- read.csv(data_file, header = TRUE, stringsAsFactors = FALSE)
print(existing_data)
data_file <- read.csv("data-analysis/data.csv")
data_file
existing_data <- read.csv(data_file, header = TRUE, stringsAsFactors = FALSE)
print(existing_data)
existing_data <- read.csv("data-analysis/data.csv")
existing_data <- read.csv(data_file, header = TRUE, stringsAsFactors = FALSE)
print(existing_data)
existing_data <- read.csv("data-analysis/data.csv")
# Set the path to the directory containing subdirectories
data_folder <- "data-analysis//data"
existing_data <- read.csv("data-analysis/data.csv")
existing_data
existing_data <- read.csv("data-analysis/data.csv")
existing_data
# Set the path to the directory containing subdirectories
data_folder <- "data-analysis//data"
# List all directories inside the 'data' folder
directories <- list.dirs(path = data_folder, full.names = TRUE, recursive = FALSE)
# Loop through each directory
for (dir in directories) {
print(dir)
# Extract digits from the directory name
digits <-as.numeric(gsub("\\D", "", basename(dir)))  # Using 'basename' to get the name of the directory
# Check if there are at least 6 digits in the directory name
if (nchar(digits) >= 6) {
# Extract the first six digits for run_id, microphone, camera, screen, duration, and app
run_id <- substr(digits, 1, 1)
microphone <- substr(digits, 2, 2)
camera <- substr(digits, 3, 3)
screen <- substr(digits, 4, 4)
duration <- substr(digits, 5, 5 )
app <- substr(digits, 6, 6)
}
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
# Add 'total packets' column to the extracted_data data frame
network <- total_packets
}
# Create a data frame with the extracted digits
extracted_data <- data.frame(run_id, microphone, camera, screen, duration, app, network, stringsAsFactors = FALSE)
print(extracted_data)
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
print(existing_data)
}
# Write the extracted data to the existing CSV file
write.csv(existing_data, file = "data-analysis/data.csv", row.names = FALSE, quote = FALSE)
# Check the updated contents of the CSV file
read.csv("data-analysis/data.csv")
existing_data <- read.csv("data-analysis/data.csv")
existing_data
# Set the path to the directory containing subdirectories
data_folder <- "data-analysis//data"
# List all directories inside the 'data' folder
directories <- list.dirs(path = data_folder, full.names = TRUE, recursive = FALSE)
# Loop through each directory
for (dir in directories) {
print(dir)
# Extract digits from the directory name
digits <-as.numeric(gsub("\\D", "", basename(dir)))  # Using 'basename' to get the name of the directory
# Check if there are at least 6 digits in the directory name
if (nchar(digits) >= 6) {
# Extract the first six digits for run_id, microphone, camera, screen, duration, and app
run_id <- substr(digits, 1, 1)
microphone <- substr(digits, 2, 2)
camera <- substr(digits, 3, 3)
screen <- substr(digits, 4, 4)
duration <- substr(digits, 5, 5 )
app <- substr(digits, 6, 6)
}
# Read packets_count.csv file inside the directory
packets_file <- list.files(dir, pattern = "packet_counts.csv", full.names = TRUE)
if (length(packets_file) > 0) {
packets_data <- read.csv(packets_file, header = TRUE, stringsAsFactors = FALSE)
# Extract the 'total packets' column
total_packets <- packets_data$Total.Packets
# Add 'total packets' column to the extracted_data data frame
network <- total_packets
}
# Create a data frame with the extracted digits
extracted_data <- data.frame(run_id, microphone, camera, screen, duration, app, network, stringsAsFactors = FALSE)
print(extracted_data)
# Append the extracted data to the existing_data data frame
existing_data <- rbind(existing_data, extracted_data)
print(existing_data)
}
# Write the extracted data to the existing CSV file
write.csv(existing_data, file = "data-analysis/data.csv", row.names = FALSE, quote = FALSE)
# Check the updated contents of the CSV file
read.csv("data-analysis/data.csv")
